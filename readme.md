# Neural Network (ANN) — Shallow vs Deep — AI Student Impact Dataset

## Overview
This project implements, tunes, and compares two distinct **Neural Network** architectures
built with **PyTorch** — a **Shallow NN** (exactly one hidden layer) and a **Deep NN**
(at least three hidden layers with dropout regularization) — on the AI Student Impact
dataset for multi-class classification (`Burnout_Risk_Level`). The full pipeline is automated
on GitHub: the dataset is fetched from a public raw URL, both models are tuned with
validation splits, and all required 2×1 side-by-side visualisations are produced without
any manual file upload.

## Dataset
- **Source file:** `ai_student_impact_dataset.csv`
- **Target:** `Burnout_Risk_Level` (multi-class: Low, Medium, High)
- **Features:** Student_ID, Major_Category, Year_of_Study, Pre_Semester_GPA,
  Weekly_GenAI_Hours, Primary_Use_Case, Prompt_Engineering_Skill, Tool_Diversity,
  Paid_Subscription, Traditional_Study_Hours, Perceived_AI_Dependency,
  Institutional_Policy, Anxiety_Level_During_Exams, Post_Semester_GPA,
  Skill_Retention_Score

## Implementation
- **Framework:** PyTorch (`torch.nn`, `torch.optim`)
- **Shallow NN:** Exactly one hidden layer. Hyperparameter-tuned on:
  - Hidden units: 32, 64, 128, 256
  - Activation functions: ReLU, Sigmoid, Tanh
  - Batch size: 32, 64, 128
- **Deep NN:** Three hidden layers with Dropout + Batch Normalization + L2 weight decay.
  Hyperparameter-tuned on:
  - Learning rate: 0.01, 0.001, 0.0001
  - Optimizer: Adam vs SGD (with momentum)
  - Epochs (up to 100)
- **Loss function:** Weighted CrossEntropyLoss (inverse-frequency class weights)
- **Regularization (Deep):** Dropout (rate=0.3), Batch Normalization, L2 weight decay

## Workflow
1. Load data from `ai_student_impact_dataset.csv` (with GitHub fallback).
2. Perform exploratory data analysis (EDA).
3. Encode categorical columns with `LabelEncoder`.
4. Stratified 70/15/15 train/validation/test split.
5. Scale features with `StandardScaler`.
6. Build and tune the **Shallow NN** (1 hidden layer) with validation split.
7. Build and tune the **Deep NN** (3+ hidden layers) with dropout.
8. Evaluate both optimised models on the test set.
9. Generate all required visualisations (2×1 comparison matrices).

## Visualisations Generated
- `screenshots/Class_Distribution.png` — target class balance
- `screenshots/Feature_Distributions.png` — numeric feature histograms
- `screenshots/Correlation_Heatmap.png` — feature correlation
- `screenshots/Training_History.png` — loss and accuracy curves (2×1 grid)
- `screenshots/Confusion_Matrix.png` — confusion matrices (2×1 grid)
- `screenshots/ROC_Curve.png` — ROC curves with AUC (2×1 grid, One-vs-Rest)
- `screenshots/Metrics_Comparison.png` — grouped bar chart of all metrics

## Project Files
- `220133_ANN.ipynb` — main notebook
- `ai_student_impact_dataset.csv` — input dataset
- `screenshots/` — plots generated by the notebook

## How to Run
1. Open `220133_ANN.ipynb` in Jupyter or Google Colab.
2. Install dependencies: `pandas`, `numpy`, `matplotlib`, `seaborn`, `scikit-learn`, `torch`.
3. Click **Runtime → Run all**.
4. The dataset is fetched automatically — no manual file uploads required.

## Notes
- Both models use weighted CrossEntropyLoss with inverse-frequency class weights.
- The Deep NN includes Dropout, Batch Normalization, and L2 weight decay to combat
  overfitting on the dataset (50,000 samples).
- Feature scaling (StandardScaler) is essential for neural network convergence.
- Multi-class ROC curves use One-vs-Rest (OvR) strategy.

---
*Student: Md. Maruf (220133)*
